# 零感调度器 · 2 分钟演示视频脚本

> 录制建议：OBS 录屏，1920×1080，深色模式桌面背景。语速偏慢，自然说话。
> BGM：轻量电子/lo-fi，音量低于人声 12dB。
> 字体叠加：PingFang SC / Microsoft YaHei，品牌紫 #7C3AED 用于关键词高亮。

---

## 分镜表

| 段 | 时长 | 画面 | 配音 |
|----|------|------|------|
| **1** | 0:00-0:08 | 黑底浮现 零感横版 Logo + "零感调度器 AIMate" 标题，紫色渐变淡入 | "你有没有想过——让 Claude Code 和 Codex 自己分工干活？零感调度器，做到了。" |
| **2** | 0:08-0:20 | 切到浏览器，地址栏输入 127.0.0.1:7799，页面加载完成。鼠标 hover Header 指示灯（绿点），然后移到费用徽章 | "打开浏览器，看到这个控制台。绿色表示 Claude 和 Codex 都装好了。费用徽章还是零——什么都没跑过。" |
| **3** | 0:20-0:35 | 在输入框键入需求："用 Python 写一个命令行天气查询工具，支持城市参数，调用 wttr.in API，把结果保存成 CSV。写完自动测试并打包成 exe。" | "给它一个真实需求。我们想写个天气查询工具——查天气、存 CSV、打包 exe。这活如果自己干，得查 API 文档、写代码、写测试、配 PyInstaller……" |
| **4** | 0:35-0:48 | 点击"分析需求"。等待 3-4 秒，拆解方案卡片弹出。镜头推进特写三个卡片：Claude 卡片（紫色左边框）、Codex 卡片（蓝色左边框）。字幕标注"分析阶段费用: ≈$0.0008" | "点分析，3 秒出方案。拆成了三个子任务：写代码和异常处理归 Claude，写测试和打包 exe 归 Codex。右上角能看到这次分析花了多少钱——不到一厘。" |
| **5** | 0:48-1:05 | 点击"执行方案"。进度条开始走，三个任务卡片依次变色：运行中（紫色闪烁）→ 完成（绿色）。第一个任务完成时右侧闪现输出预览 | "点执行，看进度。三个任务依次跑——Claude 在写代码，Codex 在等测试框架，第三个任务 Codex 上 PyInstaller 打包。每个任务跑完立即显示输出。" |
| **6** | 1:05-1:20 | 三个任务全部绿色。进度条 100%。下方结果区展开，显示三个 `<details>` 卡片。点开第一个，展示 Claude 写的完整代码 | "不到一分钟，全跑完了。展开结果看看——Claude 写的 weather.py，完整的命令行工具。再展开 Codex 的测试输出——pytest 3 passed。" |
| **7** | 1:20-1:35 | 点击 Header 的 ≈ $ 徽章，弹出费用明细面板。特写表格：时间、任务摘要、Token 数、费用。镜头扫过三行记录。关闭面板 | "点费用徽章看明细。每次分析的时间、用了多少 token、花了多少钱，一条一条清清楚楚。实际费用以 DeepSeek 账单为准，这里只是估算。" |
| **8** | 1:35-1:45 | 点右上角齿轮，设置面板弹出。特写 temperature 滑块从 0.3 拖到 0.8。然后点 ? 号，帮助面板弹出，镜头扫过五个帮助卡片后关闭 | "想换模型？点齿轮直接在浏览器改，保存生效。忘了怎么配 API Key？点问号随时查看——安全说明、模型切换、费用解释全在里面。" |
| **9** | 1:45-1:55 | 切到桌面，打开终端，输入 `cd D:\AIMate && python server.py`，回车。浏览器自动打开 127.0.0.1:7799 | "装起来也简单。克隆项目、pip install、设个环境变量，python server.py 就跑起来了。双击 start.bat 也行。" |
| **10** | 1:55-2:00 | 黑底，零感 Logo + "零门槛上手AI，激发编程灵感" + GitHub 地址 + QQ 群 956118706。画面定格 | "零感调度器，开源免费。GitHub 搜零感科技，QQ 群 956118706。让 AI 替你分工，你只需要一个想法。" |

---

## 录制检查清单

- [ ] 桌面背景设为深色/纯黑
- [ ] 浏览器缩放 100%，无书签栏
- [ ] 终端字体 Consolas 14px，深色主题
- [ ] 确保 Claude Code 和 Codex 正常（Header 绿灯）
- [ ] 提前准备好 demand 文本，录屏时粘贴
- [ ] 关闭所有通知（微信、QQ、邮件）
- [ ] 鼠标移动平滑，不要抖动
- [ ] 录完后检查音频音量、对齐画面

## 字幕叠加位置

| 时间 | 位置 | 内容 |
|------|------|------|
| 0:35 | 右上角 | "分析耗时 3.2s" |
| 0:40 | 分析结果区右下 | "≈ $0.0008" |
| 1:08 | 进度条上方 | "Claude Code 执行中…" |
| 1:20 | 结果区右下 | "3/3 全部完成" |
| 1:55 | 画面底部居中 | "github.com/Cail726/aimate" |
